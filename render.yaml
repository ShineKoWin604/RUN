services:
  - type: web
    name: reju-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python rejubot.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
